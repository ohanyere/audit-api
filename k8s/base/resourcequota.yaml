apiVersion: v1
kind: ResourceQuota
metadata:
  name: audit-api-quota
  labels:
    app.kubernetes.io/name: audit-api
    finops.ohanyere.io/cost-center: developer-experience
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 2Gi
    limits.cpu: "4"
    limits.memory: 4Gi
    pods: "10"
