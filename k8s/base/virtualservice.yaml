apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: audit-api
  labels:
    app.kubernetes.io/name: audit-api
spec:
  hosts:
    - audit-api.dev.platform.ohanyere.internal
  gateways:
    - istio-system/platform-gateway
  http:
    - route:
        - destination:
            host: audit-api
            port:
              number: 80
          weight: 100
